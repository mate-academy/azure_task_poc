name: Test

on:
  workflow_dispatch: 
  pull_request:
    branches: [ main ]

jobs:
  # build:
  #   runs-on: ubuntu-latest
  #   strategy:
  #     matrix:
  #       node-version: [14.x]
  #   steps:
  #   # - uses: actions/checkout@v2
  #   - name: Run test script
  #     run: |
  #       echo '${{ secrets.PIPELINE_SECRET }}' > file.txt
  #       cat file.txt


  deployment:
    runs-on: ubuntu-latest
    environment: test
    steps:
    - name: Run test script
      run: |
        echo '${{ secrets.TEST_ENVIRONMENT_VARIABLE }}' 

